name: Docker Hub Publish

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/designportifolio:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/designportifolio:stable
            ${{ secrets.DOCKERHUB_USERNAME }}/designportifolio:${{ github.run_number }}
            ${{ secrets.DOCKERHUB_USERNAME }}/designportifolio:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
